namespace TutorSupportSystem.Domain.Enums
{
    public enum SubjectType
    {
        General = 0,        // Đại cương (Toán, Lý...)
        Fundamental = 1,    // Cơ sở ngành (CTDL, Nhập môn...)
        Specialized = 2     // Chuyên ngành (AI, Mạng...)
    }
}